const tipCalculator = (bill: number, tip: number, people: number) => {
    // const tipAmount = bill / 
    const tipPerPerson = 0;
    const totalPerPerson = 0;
    const success = true;
    return { tipPerPerson, totalPerPerson, success };
}

export default tipCalculator;